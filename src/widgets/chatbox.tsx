import { usePlugin, renderWidget, useRunAsync, WidgetLocation, SelectionType, RichTextElementInterface, useTracker } from '@remnote/plugin-sdk';

function Chatbot() {
  const plugin = usePlugin();
  // const x = useTracker(() => , [])
  return (
    <div>
    </div>
  )
}

renderWidget(Chatbot)
